version: "1"
# A plaintext password YAML cannot parse: a plain scalar may not start with `@`.
connections:
  - id: "broken-secret"
    name: "Broken"
    type: postgres
    host: pg.internal
    password: @CanaryPlaintextPassword
    roles: ["*"]
